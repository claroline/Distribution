<script src="{{ asset('packages/video.js/dist/video.min.js') | raw }}"></script>
<script>
    videojs.options.flash.swf = "{{ asset('packages/video.js/dist/video-js.swf') | raw }}"
</script>
<script>/*
    var vids = document.getElementsByTagName('video');

    // vids is an HTMLCollection
    for (var i = 0; i < vids.length; i++) {
        player = videojs(vids.item(i).id, {
            techOrder: ['flash', 'html5'],
            autoplay: false,
            controls: true,
            preload: "metadata",
            fluid: true
        }, function() {
            //nothing
        });
    }*/

    var insertedNodes = [];

    var observer = new MutationObserver(function(mutations) {
     mutations.forEach(function(mutation) {
       console.log(mutation)
     })
    });

    observer.observe(document.body, { childList: true, subtree: true });


</script>
<link href="{{ asset('packages/video.js/dist/video-js.min.css') | raw }}" rel="stylesheet">
