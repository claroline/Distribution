{% extends "ClarolineCoreBundle:Administration:layout.html.twig" %}

{% block title %}{{ parent() ~ ' - ' ~ 'users' | trans({}, 'platform') | striptags | raw }}{% endblock %}

{% block breadcrumb %}
    {{ macros.breadcrumbs([
    {
        'name': 'administration'|trans({}, 'platform'),
        'href': path('claro_admin_index')
    },
    {
        'name': 'users_management'|trans({}, 'platform'),
        'href': ''
    }
    ]) }}
{% endblock %}

{% block content %}
    <div>{{ macros.flashBox() }}</div>
    <div
      class="page-container users-container"
      data-users="{{ users | json_encode | raw | escape }}"
      data-groups="{{ groups | json_encode | raw | escape }}"
      data-roles="{{ roles | json_encode | raw | escape }}"
    >
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src="{{ hotAsset('dist/claroline-distribution-main-core-user-management.js') }}"></script>
{% endblock %}
