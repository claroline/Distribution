
const termOfService = (state) => state.termOfService
const options = (state) => state.options
const facets = (state) => state.facets
const workspaces = (state) => state.worksapces

export const select = {
  termOfService,
  options,
  facets,
  workspaces
}
