import {connect} from 'react-redux'

import {WorkspaceRegistration as WorkspaceRegistrationComponent} from '#/main/core/workspace/components/registration'

const WorkspaceRegistration = connect(
  (state) => ({

  }),
  (dispatch) => ({

  })
)(WorkspaceRegistrationComponent)

export {
  WorkspaceRegistration
}
