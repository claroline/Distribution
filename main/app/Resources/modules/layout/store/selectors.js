
const maintenance = state => state.maintenance
const impersonated = state => state.impersonated

const menuOpened = state => state.menuOpened

export const selectors = {
  maintenance,
  impersonated,
  menuOpened
}
